import { createSlice } from "@reduxjs/toolkit";

interface Props {
  name: ''
}
const initialState = []

const basketSlice = {
  name: 'basket',
  initialState,
  reducers: {}
}

export default basketSlice.reducers